import type{ReactNode}from'react';import Link from'next/link';import{BottomNav}from'./BottomNav';import{getPublicEnv}from'../../lib/config/env';export function AppShell({children}:{children:ReactNode}){const env=getPublicEnv();return <div className="app-shell"><header className="topbar"><div><p className="eyebrow">{env.mode==='demo'?'Demo mode':'Production'}</p><h1>Super App</h1></div><Link href="/settings" className="pill">Settings</Link></header><main className="content">{children}</main><BottomNav/></div>}
