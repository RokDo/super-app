export async function getStored<T>(key:string,fallback:T):Promise<T>{if(typeof localStorage==='undefined')return fallback;const raw=localStorage.getItem(key);return raw?JSON.parse(raw) as T:fallback}export async function setStored<T>(key:string,value:T){if(typeof localStorage!=='undefined')localStorage.setItem(key,JSON.stringify(value))}export async function clearStored(key:string){if(typeof localStorage!=='undefined')localStorage.removeItem(key)}
