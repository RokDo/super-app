export type NotificationPayload={title:string;body:string};export type NotificationResult={ok:boolean;channel:string;error?:string};export interface NotificationAdapter{send(n:NotificationPayload):Promise<NotificationResult>}export const inAppNotificationAdapter:NotificationAdapter={async send(){return{ok:true,channel:'in-app'}}};export const discordNotificationAdapter:NotificationAdapter={async send(n){const url=process.env.DISCORD_WEBHOOK_URL;if(!url)return{ok:false,channel:'discord',error:'Discord webhook not configured'};const r=await fetch(url,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({content:`**${n.title}**\n${n.body}`})});return{ok:r.ok,channel:'discord',error:r.ok?undefined:'Discord request failed'}}};
