# AI Privacy

AI context must be built from minimal summaries and filtered by user settings before any provider call. Disabled categories are excluded. Do not send internal database IDs, service-role credentials, unrelated metadata, progress photos, or detailed health records unless required for the requested feature.

User-facing answers should include a Data considered section, while provider metadata remains hidden from the UI.
