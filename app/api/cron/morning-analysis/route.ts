import{NextResponse}from'next/server';import{getServerEnv}from'../../../../lib/config/env';import{detectMorningSignals}from'../../../../lib/calculations/health';import{demoBody,demoFoods,demoRecovery,demoWorkout}from'../../../../lib/repositories/demo-data';export async function POST(req:Request){const env=getServerEnv();if(env.cronSecret){const token=req.headers.get('authorization')?.replace('Bearer ','');if(token!==env.cronSecret)return NextResponse.json({error:'Unauthorized'},{status:401})}const signals=detectMorningSignals({foods:[demoFoods],body:demoBody,recovery:[demoRecovery],workouts:[demoWorkout]});return NextResponse.json({mode:env.mode,signals,summary:signals[0]??'No urgent issues detected.'})}
