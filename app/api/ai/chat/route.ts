import{NextResponse}from'next/server';import{getServerEnv}from'../../../../lib/config/env';export async function POST(req:Request){const body=await req.json().catch(()=>({message:''}));const env=getServerEnv();return NextResponse.json({provider:env.openRouterConfigured?env.aiProvider:'mock',content:`Mock coach response: ${body.message||'Ask me about training, food or recovery.'}`,dataConsidered:['demo workouts','nutrition totals','readiness score']})}
