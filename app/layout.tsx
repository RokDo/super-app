import type{Metadata,Viewport}from'next';import type{ReactNode}from'react';import'./globals.css';export const metadata:Metadata={title:'Super App',description:'Mobile-first health and fitness PWA',manifest:'/manifest.webmanifest',appleWebApp:{capable:true,title:'Super App',statusBarStyle:'default'}};export const viewport:Viewport={themeColor:'#2563eb',viewportFit:'cover'};export default function RootLayout({children}:{children:ReactNode}){return <html lang="en"><body>{children}<script dangerouslySetInnerHTML={{__html:`if('serviceWorker'in navigator){window.addEventListener('load',()=>navigator.serviceWorker.register('/sw.js').catch(()=>{}))}`}}/></body></html>}
